/** Canonical donation CTA destination — use for every Donate / Donate Now link. */
export const DONATE_SECTION_ID = "donation-options";

export const DONATE_PATH = "/donate";

export const DONATE_HREF = `${DONATE_PATH}#${DONATE_SECTION_ID}`;

export const DONATE_ANCHOR = `#${DONATE_SECTION_ID}`;
